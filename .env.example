PORT=4000
JWT_SECRET=change-this-local-secret
# Optional. If omitted, the app uses data/mes-demo.json so the MVP can run immediately.
DATABASE_URL=postgres://postgres:postgres@localhost:5432/mes_erp
UPLOAD_DIR=uploads
